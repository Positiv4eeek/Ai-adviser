name: Build and Push Docker Images

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Docker login
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build backend image
        run: docker build -t darkposss/ai-adviser-backend:latest ./backend

      - name: Build frontend image
        run: docker build -t darkposss/ai-adviser-frontend:latest ./frontend

      - name: Push backend image
        run: docker push darkposss/ai-adviser-backend:latest

      - name: Push frontend image
        run: docker push darkposss/ai-adviser-frontend:latest
